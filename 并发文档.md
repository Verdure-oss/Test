# 并发测试文档

## 概述

本文档介绍项目中涉及的并发测试方法、工具和最佳实践，旨在确保系统在高并发场景下的稳定性、正确性和性能。

---

## 1. 并发测试目标

- **功能性验证**：确保系统在多线程/高并发环境下业务逻辑正确执行。
- **性能评估**：测量系统的吞吐量、响应时间等性能指标。
- **稳定性检测**：验证系统在长时间并发压力下是否出现内存泄漏、资源耗尽等问题。
- **线程安全验证**：发现并定位竞态条件（Race Condition）、死锁（Deadlock）等并发问题。

---

## 2. 常用并发测试工具

### 2.1 JMeter

Apache JMeter 是业界最广泛使用的开源负载/压力测试工具。

```bash
# JMeter 命令行模式执行测试计划
jmeter -n -t test_plan.jmx -l result.jtl -e -o report_dir
```

**适用场景**：
- HTTP API 接口压测
- WebSocket / 数据库 / JMS 协议测试
- 生成可视化性能报告

### 2.2 Benchmark 框架

| 框架 | 语言 | 特点 |
|------|------|------|
| JMH (Java Microbenchmark Harness) | Java | Oracle 官方微基准测试框架，适合方法级精度测试 |
| ab (Apache Bench) | — | 轻量级 HTTP 压测工具 |
| wrk | C/Lua | 高性能 HTTP 压测，单进程支持高并发 |
| Gatling | Scala/Java | 基于 Actor 模型，报告精美，适合 CI 集成 |
| Locust | Python | 基于代码定义场景，易上手 |

### 2.3 Java 内置工具

- `java.util.concurrent` 包下各类并发容器和同步器
- `CountDownLatch` / `CyclicBarrier` — 用于协调多个线程同时开始
- `ExecutorService` — 线程池管理
- `ForkJoinPool` — 适用于分治任务的并行计算

---

## 3. 并发测试实施步骤

### 3.1 明确测试场景

根据项目特性定义并发场景：
1. **登录场景**：多用户同时登录，验证会话隔离
2. **数据操作场景**：同一资源的并发读写，验证数据一致性
3. **接口调用场景**：模拟多用户同时请求核心接口
4. **批量处理场景**：大批量数据并发处理，监控资源占用

### 3.2 编写并发测试用例（JMH 示例）

```java
import org.openjdk.jmh.annotations.*;
import java.util.concurrent.*;

@State(Scope.Benchmark)
@BenchmarkMode(Mode.Throughput)
@OutputTimeUnit(TimeUnit.SECONDS)
public class ConcurrentTest {

    private static final int CONCURRENCY_LEVEL = 100;

    @Setup
    public void setup() {
        // 初始化测试环境
    }

    @Benchmark
    public void testConcurrentAccess(BenchMarkState state) {
        CountDownLatch startLatch = new CountDownLatch(1);
        CountDownLatch endLatch = new CountDownLatch(CONCURRENCY_LEVEL);
        ExecutorService executor = Executors.newFixedThreadPool(CONCURRENCY_LEVEL);

        for (int i = 0; i < CONCURRENCY_LEVEL; i++) {
            executor.submit(() -> {
                try {
                    startLatch.await();           // 等待所有线程就绪
                    // 执行被测方法
                    state.doWork();
                } catch (InterruptedException e) {
                    Thread.currentThread().interrupt();
                } finally {
                    endLatch.countDown();
                }
            });
        }

        startLatch.countDown();                  // 释放所有线程
        try {
            endLatch.await();                     // 等待全部完成
        } catch (InterruptedException e) {
            Thread.currentThread().interrupt();
        }
        executor.shutdown();
    }

    public void doWork() {
        // 实际业务逻辑
    }
}
```

### 3.3 JMeter 测试计划要点

1. **线程组配置**：
   - 线程数（Users）：模拟并发用户数
   - Ramp-Up 时间：所有用户启动所需时间
   - 循环次数：每个用户的请求次数

2. **监听器**：
   - 聚合报告（Aggregate Report）— 查看吞吐量、平均响应时间
   - 图形结果（Graph Results）— 直观展示趋势

3. **断言**：
   - 添加响应断言，验证返回状态码及业务逻辑正确性

### 3.4 ab 工具快速压测

```bash
# 100个并发连接，总计10000次请求
ab -n 10000 -c 100 http://localhost:8080/test/test
```

输出关键字段解读：
- **Requests per second** — 每秒请求数（吞吐量）
- **Time per request** — 平均响应时间
- **Percentage of requests served within certain time** — 各百分位响应时间

---

## 4. 常见并发问题分析与解决

### 4.1 竞态条件（Race Condition）

**症状**：偶现的数据不一致或错误结果。

**排查手段**：
- 使用线程安全集合替代非线程安全集合
- 加锁保护共享资源（`synchronized` / `ReentrantLock`）
- 使用 `volatile` 保证可见性
- 通过 JMH 的线程干扰检测 (`org.openjdk.jmh.generators.bytecode.JmhBytecodeGenerator`)

### 4.2 死锁（Deadlock）

**症状**：程序卡死，线程无响应。

**排查手段**：
```bash
# 打印线程栈，查找阻塞点
jstack <pid> > thread_dump.txt
```
- 固定锁获取顺序
- 使用 `tryLock(timeout)` 避免无限等待
- 减少嵌套锁的使用

### 4.3 内存泄漏与 OOM

**症状**：长时间运行后系统崩溃，出现 `OutOfMemoryError`。

**排查手段**：
- 使用 MAT (Memory Analyzer Tool) 分析 heap dump
- 监控线程创建数量，及时 shutdown 线程池
- 限制线程池最大队列大小

---

## 5. CI/CD 集成建议

将并发测试集成到 Jenkins Pipeline 中：

```groovy
stage('并发测试') {
    steps {
        // 使用 JMeter
        sh 'jmeter -n -t src/test/jmeter/concurrent_test.jmx -l result.jtl'
        
        // 或使用 JMH
        sh 'mvn jmh:jmh -Djmh.include=.*ConcurrentTest.*'
    }
    post {
        always {
            junit 'result.jtl'
        }
    }
}
```

---

## 6. 性能基线与阈值建议

| 指标 | 推荐阈值 | 说明 |
|------|----------|------|
| P95 响应时间 | ≤ 500ms | 95% 请求在 500ms 内完成 |
| P99 响应时间 | ≤ 1000ms | 99% 请求在 1s 内完成 |
| 吞吐量 | ≥ 按业务要求设定 | 满足业务峰值需求 |
| 错误率 | ≤ 0.1% | 失败请求占比不超过千分之一 |
| CPU 使用率 | ≤ 75% | 留出缓冲空间 |
| 内存使用率 | ≤ 80% | 防止 OOM |

---

## 7. 测试报告模板

```
并发测试报告
============
测试日期：YYYY-MM-DD
测试负责人：XXX
版本信息：vX.X.X

【测试环境】
- JVM：Java XX
- 服务器配置：XX核 XX GB
- 并发配置：XX 线程 / Ramp-Up XX s

【测试结果摘要】
- 总请求数：XXXXX
- 成功请求：XXXXX (成功率 XX%)
- 平均响应时间：XX ms
- P95 响应时间：XX ms
- P99 响应时间：XX ms
- 吞吐量：XXX req/s

【发现的问题】
1. 问题描述 ...
   严重等级：高/中/低
   解决方案：...

【结论】
□ 通过 □ 不通过

【改进建议】
- 建议 ...
```

---

## 附录

### A. 推荐的 Maven 依赖

```xml
<!-- JMH 微基准测试 -->
<dependency>
    <groupId>org.openjdk.jmh</groupId>
    <artifactId>jmh-core</artifactId>
    <version>1.37</version>
    <scope>test</scope>
</dependency>
<dependency>
    <groupId>org.openjdk.jmh</groupId>
    <artifactId>jmh-generator-annprocess</artifactId>
    <version>1.37</version>
    <scope>test</scope>
</dependency>
```

### B. 参考资料

- [Oracle Java Concurrency in Practice](https://docs.oracle.com/javase/tutorial/essential/concurrency/)
- [Apache JMeter Documentation](https://jmeter.apache.org/usermanual/index.html)
- [JMH Official Guide](http://openjdk.java.net/projects/code-tools/jmh/)
